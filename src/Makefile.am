#ROCKSDB := $(top_builddir)/src/rocksdb/librocksdb.a

#$(ROCKSDB) :
#	@echo "***************Building rocksdb now********************" && cd $(@D) && pwd && $(MAKE) static_lib

#bin_PROGRAMS = test

#test_SOURCES = test.cpp
#test_LDADD = $(ROCKSDB)

SUBDIRS = base database ledger p2p
